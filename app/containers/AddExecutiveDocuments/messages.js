/*
 * AddExecutiveDocuments Messages
 *
 * This contains all the text for the AddExecutiveDocuments container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddExecutiveDocuments';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddExecutiveDocuments container!',
  },
});