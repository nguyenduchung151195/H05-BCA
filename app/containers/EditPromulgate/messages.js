/*
 * EditPromulgate Messages
 *
 * This contains all the text for the EditPromulgate container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EditPromulgate';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditPromulgate container!',
  },
});