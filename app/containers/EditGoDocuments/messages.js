/*
 * EditGoDocuments Messages
 *
 * This contains all the text for the EditGoDocuments container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EditGoDocuments';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditGoDocuments container!',
  },
});