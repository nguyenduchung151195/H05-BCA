/*
 * ProcessFlow Messages
 *
 * This contains all the text for the ProcessFlow container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProcessFlow';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProcessFlow container!',
  },
});