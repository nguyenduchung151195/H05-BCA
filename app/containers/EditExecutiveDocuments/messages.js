/*
 * EditExecutiveDocuments Messages
 *
 * This contains all the text for the EditExecutiveDocuments container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EditExecutiveDocuments';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditExecutiveDocuments container!',
  },
});