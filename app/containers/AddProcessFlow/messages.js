/*
 * AddProcessFlow Messages
 *
 * This contains all the text for the AddProcessFlow container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddProcessFlow';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddProcessFlow container!',
  },
});