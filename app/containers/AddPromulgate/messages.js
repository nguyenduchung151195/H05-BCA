/*
 * AddPromulgate Messages
 *
 * This contains all the text for the AddPromulgate container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddPromulgate';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddPromulgate container!',
  },
});