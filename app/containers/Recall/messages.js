/*
 * Recall Messages
 *
 * This contains all the text for the Recall container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Recall';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Recall container!',
  },
});